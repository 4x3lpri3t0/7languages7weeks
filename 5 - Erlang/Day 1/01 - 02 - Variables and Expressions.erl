2 + 2.
% 4
2 + 2.0
% 4.0

"string".

% Each statement ends in a period.

[1, 2, 3].

% 4 + "string".
% => ERROR

% Unlike Scala, there’s no coercion between strings and ints

% variable = 4
% => ERROR

% Variables must start with an uppercase letter

Var = 1.
% 1

% Var = 2.
% => ERROR

% variables begin with a capital letter, and
% they are immutable.

% You can assign each value only once